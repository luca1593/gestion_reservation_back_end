spring:
  application:
    name: gsrt
  datasource:
    url: jdbc:mysql://12.24.5.100:3306/grst?createDatabaseIfNotExist=true
    username: luca
    password: luca1593
    driver-class-name: com.mysql.cj.jdbc.Driver
    main:
      allow-circular-references: true
  webmvc:
    cors:
      allowed-origins: "*"
  jpa:
    database: MYSQL
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
      format_sql: true
      show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    dispatch-options-request: true
server:
  error:
    whitelabel:
      enabled: false
  servlet:
    application-display-name: Gestion de reservation
    context-path: /
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html